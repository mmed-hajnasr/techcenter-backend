# PostgreSQL (used by docker compose)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=techcenter

# Backend datasource (used by Spring Boot / Hibernate)
DATABASE__HOST=localhost
DATABASE__PORT=5432
DATABASE__NAME=techcenter
DATABASE__USERNAME=postgres
DATABASE__PASSWORD=postgres

DATABASE_URL=postgres://${DATABASE__USERNAME}:${DATABASE__PASSWORD}@localhost:${DATABASE__PORT}/${DATABASE__NAME}

# Optional Hibernate toggles
SPRING_JPA_HIBERNATE_DDL_AUTO=update
SPRING_JPA_SHOW_SQL=false
SPRING_JPA_FORMAT_SQL=true

# Optional Redis/Jaeger ports
REDIS_PORT=6379
JAEGER_VERSION=2.10.0
JAEGER_LOG_LEVEL=info

# MinIO (object storage for researcher photos and publication PDFs)
MINIO_ROOT_USER=minio
MINIO_ROOT_PASSWORD=minio12345
MINIO_PORT=9000
MINIO_CONSOLE_PORT=9001
MINIO_ENDPOINT=http://localhost:9000
MINIO_PHOTO_BUCKET=techcenter-photos
MINIO_PDF_BUCKET=techcenter-pdfs
MINIO_PRESIGN_EXPIRY_SECONDS=3600

OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4317

JWT_SECRET=wIO98lY/jGTso2IxRTsgNuT9yz1OWHvzizFo+Vw6nueYg9jXy/Pk4xnsJK5X/PCd3cOV+cIQQh1DQOy8sk3mOg==
